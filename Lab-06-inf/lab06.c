#include <stdio.h>
#include <stdlib.h>

int main(){

    float liczba, suma;
    char c;
    printf("Suma dowolnego zbioru liczb - zero = koniec:");
    suma = 0;
    while((c=getchar())!='\n'&&c!=EOF);


    return 0;
}